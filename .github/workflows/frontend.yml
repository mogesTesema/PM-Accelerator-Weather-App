name: Frontend CI

on:
  push:
    branches: [main]
    paths: ["frontend/**"]
  pull_request:
    branches: [main]
    paths: ["frontend/**"]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      # TODO: Uncomment and configure when frontend is initialized
      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: "20"
      #     cache: "npm"
      #     cache-dependency-path: frontend/package-lock.json

      # - name: Install dependencies
      #   run: npm ci

      # - name: Lint
      #   run: npm run lint

      # - name: Run tests
      #   run: npm test

      # - name: Build
      #   run: npm run build

      - name: Placeholder
        run: echo "Frontend CI will be configured when the frontend is initialized."
